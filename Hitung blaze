#include <stdio.h>

int main () {
    int n, blaze;
    int hasil = 0;
    
    printf("Masukkan jumlah angka: ");
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &blaze);
        hasil += blaze;
    }
    printf("%d", hasil);
}
