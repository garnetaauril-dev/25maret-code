#include <stdio.h>

int jumlahGenap(int n) {
    int total = 0;
    for (int i = 1; i <= n; i++) {
        if (i % 2 == 0) {
            total += i;
        }
    }
    return total;
}

int main() {
    printf("%d\n", jumlahGenap(6)); // 2+4+6 = 12
    return 0;
}
